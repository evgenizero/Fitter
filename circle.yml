machine:
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
    GRADLE_OPTS: -Xmx512m

test:
  override:
    - ./gradlew assembleDebug
    - cp -r evgenizero/Fitter/build/outputs $CIRCLE_ARTIFACTS